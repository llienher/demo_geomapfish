sudo: false

language: node_js

node_js:
- '4'

addons:
  apt:
    packages:
    - python-virtualenv

script:
- virtualenv --no-site-packages .
- bin/pip freeze
- bin/pip install 'pip==7.1.2'
- bin/pip install flake8
- bin/flake8 demo
